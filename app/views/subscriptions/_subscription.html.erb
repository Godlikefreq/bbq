<span class="subscription">
  <% if subscription.user.present? %>
    <%= render partial: "users/user", object: subscription.user %>
  <% else %>
    <em><%= subscription.user_name %></em>
  <% end %>
  <% if current_user_can_edit?(subscription) %>
    <%= link_to '<i class="bi bi-trash3"></i>'.html_safe, event_subscription_path(subscription.event, subscription),
                data: { turbo_method: :delete, turbo_confirm: t("turbo.confirm") } %>
  <% end %>
</span>
